<!doctype html>
<html lang="en">
    <head>
        <script async src="https://www.googletagmanager.com/gtag/js?id=UA-107142833-1"></script>
        <script>
            window.dataLayer = window.dataLayer || [];
            function gtag(){dataLayer.push(arguments)};
            gtag('js', new Date());
            gtag('config', 'UA-107142833-1');
        </script>
        <meta charset="utf-8">
        <title>Landon Kuhn</title>
        <meta name="description" content="Resume and work-related landing spot for Landon Kuhn">
        <meta name="author" content="Landon Kuhn">
        <meta name="viewport" content="width=400, initial-scale=1">
        <link href="https://fonts.googleapis.com/css?family=Helvetica" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css?family=Source+Sans+Pro" rel="stylesheet">
    </head>
    <style>
        body {
            font-family: 'Source Sans Pro';
            max-width: 800px;
            min-width: 400px;
            margin: auto;
            padding: 1em;
            color: #555;
        }
        h1, h2, h3, h4 {
            font-family: "Helvetica";
            color: #333;
        }
        a, a:visited, a:hover, a:active {
            color: inherit;
        }
    </style>
<body>
    <h1>{{& name }}</h1>
    <p>
        {{& city}}<br>
        {{& phone}}<br>
        {{& email}}<br>
        {{& resumeLink}}<br>
    </p>

    <p>{{& status}}</p>

    <h2>Summary</h2>
    {{#summaryStatements}}
    <p>{{& .}}</p>
    {{/summaryStatements}}

    <h2>Experience</h2>
    {{#experience}}
    <h4>{{& name}}</h4>
    <em>{{& title}} - {{& location}} - {{& time}}</em>
    <blockquote>{{& companySummary}}</blockquote>
    {{#narrative}}
    <p>{{& .}}</p>
    {{/narrative}}
    {{/experience}}
    
    <h2>Education</h2>
    <em>{{& education.where}} - {{& education.what}} - {{& education.when}}</em>
    <p>{{& education.statement}}</p>
</body>
</html>
